# Docker environment variables for the Task Manager app
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
POSTGRES_DB=taskdb
DATABASE_URL="postgresql://admin:password@localhost:5432/taskdb?schema=public"
JWT_SECRET=replace_this_with_a_strong_secret
